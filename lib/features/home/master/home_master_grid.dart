/// 3x2 discovery - cinematic tiles + real feature routes.
library;

import 'package:flutter/material.dart';

import '../reference/home_reference_module_grid.dart';

class HomeMasterGrid extends StatelessWidget {
  const HomeMasterGrid({super.key});

  @override
  Widget build(BuildContext context) => const HomeReferenceModuleGrid();
}
