/// Home header - official brand + real gems/menu wiring.
library;

import 'package:flutter/material.dart';

import '../reference/home_reference_header.dart';

class HomeMasterHeader extends StatelessWidget {
  const HomeMasterHeader({super.key});

  @override
  Widget build(BuildContext context) => const HomeReferenceHeader();
}
