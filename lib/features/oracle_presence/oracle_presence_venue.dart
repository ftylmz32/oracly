/// OR-436 — Where observatory whispers may appear.
library;

/// Screen context — each venue draws from its own atmospheric pool.
enum OraclePresenceVenue {
  /// Main observatory — beneath the hero orb.
  home,

  /// Tarot chamber — beneath the ritual subtitle.
  tarot,
}
