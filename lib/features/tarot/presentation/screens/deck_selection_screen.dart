/// Deck ready — one Oracly deck (catalogue preserved for economy).
library;

import 'package:flutter/material.dart';

import '../../ritual/screens/tarot_ritual_deck_ready_screen.dart';

class DeckSelectionScreen extends StatelessWidget {
  const DeckSelectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const TarotRitualDeckReadyScreen();
  }
}
