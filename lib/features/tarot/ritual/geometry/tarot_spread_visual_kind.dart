/// Phase 7C — presentation-only spread visual geometry kinds.
///
/// Distinct from [SignatureGeometryHook] (product metadata). Maps onto those
/// hooks where they exist; adds seven / celtic for presentation only.
library;

/// Visual layout identity for settled slots + flight targets.
enum TarotSpreadVisualKind {
  single,
  threeLinear,
  fiveLinear,
  fiveDecision,
  seven,
  celticCross,
}
