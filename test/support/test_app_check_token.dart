/// Shared test App Check token for production proxy transports.
library;

/// Placeholder only — never a real Firebase App Check token.
Future<String?> testAppCheckToken() async => 'test-app-check-token';
